# v0.2 审查报告（阶段1-4）

> 审查者：AI Code Reviewer
> 审查日期：2026-06-29 ~ 2026-07-02
> 审查基准：v0.2-PROGRESS-CHECKLIST.md

---

## 阶段1：可视化能力（S1.1-S1.3 + I1.1-I1.3）

### S1.1 visualize 工具

**判定：通过**

- `src/tools/visualize.ts`：7 种图表（bar/line/scatter/histogram/pie/box/heatmap）
- `src/engine/python-stateless.ts`：无状态 Python 调用引擎
- `scripts/generate_chart.py`：matplotlib 生成 PNG
- 安全层接入：`checkSql` + `isReadOnly` 双重检查
- 大结果处理：DuckDB COPY TO 临时 CSV → Python 读取
- 失败 fallback：返回 CSV 路径 + 错误原因
- 测试：26 个断言全部通过

### S1.2 show_image 工具

**判定：通过**

- `src/tools/show-image.ts`：PNG/SVG 展示 + magic bytes 验证
- 路径白名单 + 文件存在性 + 图片格式检查
- 文件大小限制（2MB）
- image content + text fallback 双返回
- 测试：13 个断言全部通过

### S1.3 visualization Skill

**判定：通过**

- `skills/visualization/SKILL.md`：触发条件 + 图表选择规则 + 字段选择规则
- 3 个反问模板，16 个选项，每个带 impliedAssumption
- `skills/visualization/scripts/chart-templates.sql`：7 种图表 SQL 模板

### I1.1-I1.3 集成验证

**判定：通过**

- I1.1：visualize → show_image 链路 9/9 通过
- I1.2：Skill 交互流程代码逻辑通过（需 Pi CLI 手动验证）
- I1.3：v0.1 回归 57/57 通过

---

## 阶段2：口径快照（S2.1 + I2.1-I2.3）

### S2.1 agent.md 口径回写

**判定：通过**

- `ask-clarification.ts`：选择后调用 `writeCaliberToAgentMd()`
- `persistence.ts`：saveCaliber/loadCalibers/hasCaliberForQuestion/getRecentCalibers
- `index.ts`：`before_agent_start` 读取 + 注入 system prompt
- 去重：同一 question 更新而非追加
- 截断：保留最近 10 条
- 错误容忍：写入失败只 warning，不阻塞查询
- 测试：19 个断言全部通过

### I2.1-I2.3 集成验证

**判定：通过**

- I2.1：第一次反问 → 选择 → agent.md 写入 6/6 通过
- I2.2：第二次复用口径 3/3 通过
- I2.3：v0.1 回归 2/2 通过

---

## 阶段3：过时闸 + 失败查询（S3.1-S3.2 + I3.1-I3.4）

### S3.1 过时闸最小版

**判定：通过**

- `DataDictionaryEntry.schemaFingerprint`：MD5 基于 schema + rowCount
- `index.ts`：全局 fingerprint（所有表组合 MD5）
- `query-memory.ts`：recallRelevantQueries 过滤 fingerprint 不匹配的条目
- Schema Change Detected 标注
- load_data/describe_data 后静默刷新 fingerprint
- 测试：8 个断言全部通过

### S3.2 失败查询入库最小版

**判定：通过**

- `classifyError`：4 类 + unknown（syntax_error/not_found/permission/timeout）
- 独立 `failedQueries` 数组，不影响成功查询容量闸
- 最多 10 条，淘汰最旧
- 不注入 prompt
- 测试：24 个断言全部通过

### I3.1-I3.4 集成验证

**判定：通过**

- I3.1：schema 变更 → outdated 检测 7/7 通过
- I3.2：失败查询分类入库 4/4 通过
- I3.3：淘汰 3/3 通过
- I3.4：v0.1 回归 2/2 通过

---

## 阶段4：Eval 扩展（S4.1-S4.2）

### S4.1 自动化测试

**判定：通过**

- `src/eval/run-all-v2.ts`：6 组测试，147 断言全部通过
- `acceptance-test.sh`：55/55 全部通过
- JSON 报告输出到 `.pi-data-agent/eval/report-v2.json`

### S4.2 人工验收

**判定：通过（自动化部分）**

- `acceptance-v2.sh`：25/25 全部通过
- 覆盖 8/10 项完全 + 2/10 项部分（4.2.5/4.2.7 需 Pi CLI 手动验证）

---

## 阶段5：Stretch Goal（S5.1-S5.4）

### S5.1 connect_database

**判定：通过**

- 仅支持 SQLite，DuckDB ATTACH
- 路径白名单 + 文件存在性 + alias 生成
- 多级 fallback schema 查询
- attach 失败不影响 CSV 主流程
- 测试：14 个断言全部通过

### S5.2 PII 最小脱敏

**判定：通过**

- `src/pii-guard.ts`：email/phone/id_card 三种 PII
- 纯正则，零外部依赖
- data-dictionary.ts 接入，sampleValues 脱敏
- DuckDB 原始数据不改变
- 测试：35 个断言全部通过

### S5.3 audit log

**判定：通过**

- `src/audit-log.ts`：JSONL 格式，append-only
- 白名单工具：query_data/load_data/transform_data/export_result/visualize/connect_database
- summary 脱敏，不含样本/连接串/PII
- index.ts 接入 tool_execution_start/end
- 测试：17 个断言全部通过

### S5.4 data-cleaning Skill

**判定：通过**

- `skills/data-cleaning/SKILL.md`：4 类检查策略 + 3 套反问模板
- `skills/data-cleaning/scripts/cleaning-checks.sql`：缺失/重复/异常/格式 SQL 模板
- 分级处理建议

---

## 审查总结

| 阶段 | 判定 | 断言数 |
|------|------|--------|
| 阶段1 可视化 | ✅ 通过 | 48 |
| 阶段2 口径快照 | ✅ 通过 | 30 |
| 阶段3 过时闸 | ✅ 通过 | 48 |
| 阶段4 Eval | ✅ 通过 | 147 + 25(acceptance) |
| 阶段5 Stretch | ✅ 通过 | 66 |
| **合计** | **全部通过** | **249 + 25** |
