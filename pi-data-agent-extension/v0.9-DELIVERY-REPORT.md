# v0.9 交付报告（补记）

> 依据：specs/v0.9-EXECUTION_SPEC.md（A-1 Excel 加载 / A-2 上传自动建表 / A-3 环境自检+欢迎引导 / A-4 日志分级 / A-5 数据体检卡）
> 补记日期：2026-08-23（**本报告为 v0.11 期间补记（D-1），非当时原始交付文档**；v0.9 当时的工程痕迹以 src 模块、测试与代码注释为准还原）
> 测试基线：以当前 main 分支实测为准 —— vitest 17 文件 / 124 用例全绿，`npx tsc --noEmit` 零错误（2026-08-23 实测；本文所列 v0.9 测试文件均在 vitest 套件中运行）
> 代码位置：`pi-data-agent-extension/src/`

---

## 各 Task 交付判定

| Task | 内容 | 判定 | 核心证据 |
|------|------|------|----------|
| A-1 | load_data 支持 Excel（.xlsx/.xls） | ✅ 完成 | `src/tools/load-data.ts` |
| A-2 | Dashboard 上传后自动加载进 DuckDB | ✅ 完成 | `src/dashboard/routes/upload.ts` |
| A-3 | session_start 环境自检 + 欢迎引导 | ✅ 完成 | `src/utils/env-check.ts` + `src/index.ts` |
| A-4 | 日志分级（debug 默认静默） | ✅ 完成 | `src/utils/logger.ts` + `src/config.ts` |
| A-5 | 加载后数据体检卡 + 建议问题 | ✅ 完成 | `src/hooks/data-profile.ts` |

**5 个 Task 全部落地。**

---

## 逐项证据

### A-1：Excel 加载（P0）✅

| 验收要点 | 状态 | 证据 |
|----------|------|------|
| SheetJS Node 侧解析（xlsx 包） | ✅ | `load-data.ts:21` `import * as XLSX from "xlsx"`；Buffer 读取 → `XLSX.read(buffer)` → 转 CSV → 复用 CSV 加载路径（`:166-216`） |
| zip bomb 防护（cell 数上限） | ✅ | `load-data.ts:37-38` `MAX_EXCEL_CELLS = 5_000_000`（注释："Excel 解析防护：单文件总 cell 数上限（防 zip bomb）"），超限拒绝（`:150`） |
| `sheet_name` 参数 | ✅ | `load-data.ts:60` 工具参数定义；省略时取第一个 sheet（`:196`），指定不存在的 sheet 返回人话错误并列出可用 sheet（`:198-202`） |
| details 附 `availableSheets` | ✅ | `load-data.ts:128/143` 类型定义，`:176` 从 workbook 取 SheetNames，`:222/390` 回填 details |
| magic bytes 兜底识别 | ✅ | `load-data.ts:81/156`（PK\x03\x04 zip 容器 / OLE2 D0 CF 11 E0），伪装扩展名的坏文件报清晰错误 |
| 测试 | ✅ | `src/eval/load-xlsx.test.ts`（多 sheet、类型、坏文件等，vitest 在跑） |

### A-2：上传自动建表（P0）✅

| 验收要点 | 状态 | 证据 |
|----------|------|------|
| 上传后自动调用加载函数建表 | ✅ | `upload.ts:77` `export async function autoLoadUploadedFile(...)`，内部复用与 load_data 共用的 `loadFileIntoTable`（单一实现，未漂移） |
| 响应含 `loaded{tableName,rowCount,columnCount,replaced}` | ✅ | `upload.ts:69` `AutoLoadResult` 类型；失败分支返回 `{ ok:false, reason }` 人话原因 |
| >100MB 跳过自动加载 | ✅ | `upload.ts:88-91` `AUTOLOAD_MAX_SIZE` 上限保护（注释："engine 为串行队列，避免阻塞 Dashboard 响应"），reason 提示手动加载 |
| 加载失败不影响上传本身 | ✅ | `upload.ts:134-137` 全量 try/catch，任何失败返回 reason 不抛异常 |
| 测试 | ✅ | `src/eval/upload-autoload.test.ts`（建表、失败 reason、跳过逻辑，vitest 在跑） |

### A-3：环境自检 + 欢迎引导（P1）✅

| 验收要点 | 状态 | 证据 |
|----------|------|------|
| 自检模块（DuckDB / LLM / Python 绘图） | ✅ | `src/utils/env-check.ts`（5.1KB）：`runEnvCheck({ engine, llmConfigured, checkPython })` + `formatWelcomeMessage(envCheck)` |
| session_start 异步执行一次，不阻塞启动 | ✅ | `index.ts:186-194`：session_start 中构造 PythonStatelessEngine → `runEnvCheck(...)` → 结果存入 `rt.sessionData.set("env_check", envCheck)` |
| 输出通道优先 ctx.ui.notify，回退 logger.info | ✅ | `index.ts:194-199`：`ctx.ui?.notify(lines.join("\n"), "info")`，不可用时逐行 `logger.info` |
| 自检结果供后续复用（visualize 错误附带安装指引） | ✅ | 结果缓存于 runtime.sessionData("env_check") |
| 测试 | ✅ | `src/eval/env-check.test.ts`（logger 开关行为、自检结果、文案，vitest 在跑） |

### A-4：日志分级（P1）✅

| 验收要点 | 状态 | 证据 |
|----------|------|------|
| `createLogger(scope)` 统一前缀 `[pi-data-agent]` | ✅ | `src/utils/logger.ts`：debug/info/warn/error 四级，PREFIX 常量 |
| debug 仅在开关开启时输出 | ✅ | `logger.ts:30-32` `readDebugFromEnv()`：`PI_DATA_AGENT_DEBUG=1/true` 时开启，默认静默输出到 stderr |
| config.debug 字段合并 | ✅ | `config.ts:42` `/** 调试日志开关（env PI_DATA_AGENT_DEBUG 或 config.json，默认 false）*/ debug: boolean`；`setLoggerDebug()` 由 session_start 加载 config 后调用（env 与 config 合并） |
| index.ts console.log 降噪 | ✅ | index.ts 现存日志均为 `logger.debug/info/warn/error` 形态（grep 无裸 console.log 业务输出） |

### A-5：数据体检卡 + 建议问题（P1）✅

| 验收要点 | 状态 | 证据 |
|----------|------|------|
| `profileTable(engine, tableName)` 结构化画像 | ✅ | `data-profile.ts:109` `export async function profileTable(...)`（基于 SUMMARIZE + 补充查询：缺失率/低基数列/时间跨度/主键嫌疑/恒定列） |
| 紧凑体检卡文本（人话 ≤8 行） | ✅ | `data-profile.ts:251` `export function formatProfileCard(profile)` |
| 接入点 1：load_data 成功后 best-effort | ✅ | `load-data.ts:567-573`：content 追加 `formatProfileCard(profile)`，details 附结构化 `dataProfile`（`:598`）；try/catch 单步容错，失败静默 |
| 接入点 2：Dashboard 上传链路 | ✅ | `upload.ts:119-122`（画像失败静默省略 dataProfile，不影响上传/建表） |
| 大表降级（估算行数入参） | ✅ | `upload.ts:122-124` 传入 `estimatedRowCount`；load-data 同参 |
| 测试 | ✅ | `src/eval/data-profile.test.ts`（缺失率阈值、主键重复、建议问题生成等，vitest 在跑） |

---

## 与 spec 的偏差说明

| 项 | 偏差 | 说明 |
|----|------|------|
| A-2 响应字段 | `loaded` 后续追加了 `dataProfile?` | 非 v0.9 范围，系 v0.10 A-2（上传→预览→体检卡闭环）增量，见 v0.10-REVIEW-REPORT.md |
| A-5 接入点 | 上传路由也接入了体检卡 | 同上，v0.10 复用 v0.9 的 profileTable 能力 |

## 编译与测试（当前 main 实测，2026-08-23）

| 检查项 | 结果 |
|--------|------|
| `npx tsc --noEmit` | ✅ 0 error |
| `npm test`（vitest） | ✅ 17 文件 / 124 用例全绿，约 1.7s |
| 其中 v0.9 相关测试 | ✅ load-xlsx / upload-autoload / env-check / data-profile 均在套件内通过 |

---

## 结论

v0.9 五个 Task（A-1~A-5）全部在当前 main 中可追溯、可验证，无回退迹象。判定：**✅ 交付完成**（补记口径）。
