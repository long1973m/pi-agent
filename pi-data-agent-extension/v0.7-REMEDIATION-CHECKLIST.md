# v0.7 整改清单

> 生成日期：2026-07-19
> 基准文档：v0.7-EXECUTION_SPEC.md
> 审查报告：v0.7-REVIEW-REPORT.md（终审）

---

## 一、测试修复（1 项）

| # | 文件 | 问题 | 操作 |
|---|------|------|------|
| T1 | `src/eval/report-quality-gate.test.ts` | "覆盖率低于 50% 且有 findings → warning" 用例与修复后的逻辑不一致（现改为 error） | 更新该用例期望为 error 级别 |

---

## 二、Dashboard 前端 — 报告中心（4 项）

| # | 文件 | Spec 条款 | 问题 | 操作 |
|---|------|-----------|------|------|
| F1 | `src/dashboard/static/app.js` ReportsModule | §9.1 | 分类切换按钮文案为 "Session/Analysis"，应为 "全部 / 正式分析报告 / 分析过程记录" | 修改按钮文案 |
| F2 | `src/dashboard/static/app.js` ReportsModule | §9.2 | 正式报告卡片缺来源 Session 可点击链接 | 在卡片 meta 区增加 `<a href="#" onclick="ReportsModule.openSessionReport('${r.sourceSessionId}')">来源分析</a>` |
| F3 | `src/dashboard/static/app.js` ReportsModule | §9.2 | 正式报告卡片缺 [打开][打印][查看分析过程] 三个独立按钮 | 替换单一 onclick 为三个按钮：`window.open`(打开)、`window.print()`(打印)、`openSessionReport`(查看过程) |
| F4 | `src/dashboard/static/app.js` ReportsModule | §9.3 | 过程记录卡片缺 [生成正式报告] 按钮和已生成正式报告列表 | Session 卡片中增加按钮调用 `POST /api/reports/:sessionId/generate-analysis`；渲染 `r.analysisReportIds` 列表 |

---

## 三、Dashboard 前端 — 字典页面（2 项）

| # | 文件 | Spec 条款 | 问题 | 操作 |
|---|------|-----------|------|------|
| F5 | `src/dashboard/static/app.js` DictionariesModule | §13.1 | 推断依据（evidence 列表）和不确定点未展示 | 在字段行增加展开区域，显示 `col.suggestion.evidence` 和 `col.suggestion.uncertainties` |
| F6 | `src/dashboard/static/app.js` + `style.css` | §13.2 | 缺筛选按钮：[全部] [待确认] [高置信度] [低置信度] [不确定] [用户已修正] | 增加筛选 Tab UI（filter-tab 样式 + JS 切换逻辑 + 请求参数 `status` 筛选） |

---

## 四、后端 — 数据完整性（4 项）

| # | 文件 | Spec 条款 | 问题 | 操作 |
|---|------|-----------|------|------|
| B1 | `src/dictionary/inference/suggestion-schema.ts` | §10.3 | `sourceSchemaRevision` 在校验阶段要求非空，但模型被指示留空、服务端校验后才填充，导致有效建议被误拒 | 方案 A：从 required 校验中移除 sourceSchemaRevision（推荐）；方案 B：在校验前先填充缺失字段 |
| B2 | `src/dictionary/inference/collect-inference-context.ts` | §10.2 | `dbComment` 未从 DuckDB schema 获取，导致置信度加分"有数据库注释 (+0.2)"永远不生效 | 在列构建循环中从 DuckDB schema 提取列注释并赋值到 `col.dbComment` |
| B3 | `src/dashboard/services/report-index.ts` | §8.2 | `ManifestReport` 接口和 `AddReportInput` 缺少 `evidencePath` 字段 | 接口新增 `evidencePath?: string`，`toReportSummary` 映射该字段，`addReport` 传入 |
| B4 | `src/dashboard/services/report-index.ts` | §3.3 | `addReport` 同 ID 直接覆盖，违反"正式报告不覆盖旧版本"要求 | 对 `type=analysis` 的记录，同 ID 时改为生成新唯一 ID 或拒绝覆盖 |

---

## 五、后端 — 质量门槛（1 项）

| # | 文件 | Spec 条款 | 问题 | 操作 |
|---|------|-----------|------|------|
| Q1 | `src/report/analysis/report-quality-gate.ts` | §7.1 | 结论覆盖（每条执行摘要至少关联 findingRef）、推测标识（hypothesis 标注）、口径一致性 检查为 warning 而非 error | 将这三项从 warnings 提升为 errors，与 Spec §7.1 表格对齐 |

---

## 六、P2 低优先级（2 项）

| # | 文件 | 问题 | 操作 |
|---|------|------|------|
| P1 | `src/report/evidence/evidence-validator.ts` | 空 findings 时 coverage 返回 1.0 | findings 为空时返回 0 或特殊值 N/A |
| P2 | `src/report/evidence/build-evidence-package.ts` | QueryEvidence.columns 始终为空数组 | 从 transcript 工具结果中提取列信息 |

---

## 优先级排序

**第一优先（阻断严格对齐 Spec）**：T1, B1, B2, B3, B4, Q1
**第二优先（前端完整性）**：F1-F6
**第三优先（数据质量）**：P1, P2

---

## 执行验证

修复完成后运行：

```bash
npm run check
npm run build
npx tsx src/eval/run-all-v2.ts
npx tsx src/eval/dashboard-api.test.ts
npx tsx src/eval/dashboard-storage.test.ts
npx tsx src/eval/dashboard-security.test.ts
npx tsx src/eval/analysis-report.test.ts
npx tsx src/eval/report-quality-gate.test.ts
npx tsx src/eval/dictionary-inference.test.ts
npx tsx src/eval/dictionary-review.test.ts
```

全部通过后可安排终审。